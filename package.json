{
  "version": "0.0.0",
  "name": "my-lib",
  "scripts": {
    "dev": "vite",
    "build": "vite build && vite build --mode=lib",
    "preview": "vite preview"
  },
  "devDependencies": {
    "sass": "^1.69.4",
    "vite": "^4.5.0"
  }
}
